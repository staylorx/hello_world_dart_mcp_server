name: hello_world_mcp_server
description: A simple Hello World MCP server written in Dart
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  args: ^2.4.2
  json_annotation: ^4.8.1

dev_dependencies:
  build_runner: ^2.4.7
  json_serializable: ^6.7.1
  lints: ^6.0.0

executables:
  hello_world_mcp_server:
